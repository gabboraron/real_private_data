'use strict';

class IHash extends AbstractClass {
    abstractMethods = [
          "string"// (str: string): string
        , "array" //(str: string): array
        , "preSalt"
        , "postSalt"
    ];
};